export enum ChannelType {
  /*
   * Type channel
   * MAIN is the base channel who can't be edit/delete
   * SUB is for a futur use for Sub-channel
   */
  MAIN = 'MAIN',
  NOTMAIN = 'NOTMAIN',
  SUB = 'SUB',
}
